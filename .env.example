# Copy this file to .env and fill in your values.
# .env is in .gitignore — never commit real credentials.

# ── AWS Credentials ──────────────────────────────────────
# Option A: explicit key/secret (works everywhere)
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# Required when using temporary credentials (STS / SSO)
# AWS_SESSION_TOKEN=your_session_token_here

# ── AWS Region ───────────────────────────────────────────
AWS_DEFAULT_REGION=us-east-1

# ── Notes ────────────────────────────────────────────────
# Option B: named AWS profile
#   Leave the variables above empty and instead rely on
#   the ~/.aws volume mount in docker-compose.yml.
#   The app will pick up whatever profile is set as default
#   in your ~/.aws/config, or you can select a profile in
#   the Settings panel inside the UI.
